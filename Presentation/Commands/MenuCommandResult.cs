namespace CommerceConsole.Presentation.Commands;

/// <summary>
/// Represents the result of a menu command execution.
/// </summary>
public enum MenuCommandResult
{
    /// <summary>
    /// Continue in current menu loop.
    /// </summary>
    Continue,

    /// <summary>
    /// Exit current menu loop.
    /// </summary>
    ExitMenu
}
